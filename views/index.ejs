<%- include("partials/header.ejs")  %>

<div class="container">
    <h1 class="titulo-fecha">
        <%=dia + ", "+ mes + " "+ day %> 
    </h1>

    <%if(locals.tareas){%>
        <%   for (let index = 0; index < tareas.length; index++) {%>
            <ul class="list-group">
                <li class="list-group-item">
                    <input type="checkbox" class="checkbox">
                    <span class="tachable"><%= tareas[index] %></span>
                </li>
        </ul>
    <% }%>
    <%} %>
    
    <form action="/submit" method="POST" class="input-group formulario-agregar">
        <input type="text" name="tarea" placeholder="Agregar tarea" class=" form-control input-agregar">
        <div class="input-group-append">
        <button type="submit" class="btn btn-primary boton-agregar">+</button>
        </div>
    </form>
</div>



<%- include("partials/footer.ejs")  %>

